machine:
  # Start with Python 2
  python:
    version: 2.7.5

test:
  pre:
    - (pyenv global 3.5.0 && pip install -r requirements.txt)
  override:
    - py.test
    - (pyenv global 3.5.0 && python3 -m py.test)
